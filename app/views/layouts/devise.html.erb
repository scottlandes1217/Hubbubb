<!DOCTYPE html>
<html>
  <head>
    <title>Login | hubbubb</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Resource hints for faster CDN loading -->
    <link rel="preconnect" href="https://cdn.jsdelivr.net" crossorigin>
    <link rel="dns-prefetch" href="https://cdn.jsdelivr.net">
    
    <!-- Preload logo for faster rendering (high priority) -->
    <%= preload_link_tag asset_path("hubbub-logo-large.png"), as: :image, fetchpriority: "high" %>

    <!-- Critical CSS loaded first - only Bootstrap for login page -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin>

    <!-- Application CSS (minimal for login) -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Minimal JavaScript - defer loading to not block rendering -->
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render 'shared/global_alert' if defined?(render) %>
    <%= yield %>
  </body>
</html>

